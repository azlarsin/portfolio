# Absolute public origin used to resolve Open Graph and Twitter image URLs.
NEXT_PUBLIC_SITE_URL=https://demo.example.com

# Local-only escape hatch for `pnpm build`; never set this in production.
# ALLOW_LOCAL_DEMO_URL=1
